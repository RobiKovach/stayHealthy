const swiper = new Swiper('.swiper', {
  // Optional parameters
  loop: true,
  slidesPerView: 3,
  grabCursor: true,

  // And if we need scrollbar
  scrollbar: {
    el: '.swiper-scrollbar',
  },
});